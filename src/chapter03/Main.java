package chapter03;

public class Main {


    /**
     *      함수형인터페이스
     *      Predicate   ->  test
            Consumer    ->  forEach
            Function    ->  map

            Runnable    ->
            Action      ->

            함수형 인터페이스가 필요로 하는곳에만 람다표현식 사용 가능
            and(), or(), negate()
            andThen(), compose()
     */
}
